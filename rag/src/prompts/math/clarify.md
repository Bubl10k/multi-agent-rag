The math problem is ambiguous. Ask the user a specific question to resolve it.

Identified components:
  Knowns: ${knowns}
  Unknowns: ${unknowns}
  Constraints: ${constraints}

Ask a single, clear question that will let you proceed with solving.